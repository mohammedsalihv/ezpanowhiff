<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>E-commerce Landing Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .hero-section {
            position: relative;
            text-align: center;
            color: white;
            height: 100vh; /* Make the hero section cover the entire viewport height */
            overflow: hidden;
        }

        .hero-section p {
            color: gray; /* Ensure the text color contrasts with the background */
            position: absolute;
            bottom: 10px; /* Adjust bottom spacing */
            width: 100%;
            text-align: center; /* Center the text */
            font-size: 13px; /* Adjust font size as needed */
            z-index: 10; /* Ensure it is above other elements */
            font-weight: 900;
        }

        .hero-image {
            width: 100%;
            height: 100%; /* Cover the entire hero section */
            object-fit: cover; /* Ensure the image covers the area without distortion */
        }

        .nav-overlay {
            position: absolute;
            top: 0;
            width: 100%;
            display: flex;
            justify-content: space-between;
            padding: 20px;
            z-index: 10; /* Ensure the navigation is above the image */
        }

        .nav-overlay ul {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
        }

        .nav-overlay li {
            margin-left: 20px;
        }

        .nav-overlay a {
            color: white;
            text-decoration: none;
            font-size: 16px;
        }

        .nav-overlay a:hover {
            text-decoration: underline;
        }

        .shop-button {
            font-style: italic;
            display: inline-block;
            padding: 15px 30px; /* Increased padding for wider button */
            background: transparent;
            color: #b31111;
            border: 2px solid #b31111;
            border-radius: 5px;
            cursor: pointer;
            font-size: 24px; /* Adjust font size for better readability */
            position: absolute;
            top: 80%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-decoration: none;
            font-weight: bolder;
            z-index: 10; /* Ensure the button is above the image */
        }

        .shop-button:hover {
            background: #b31111;
            color: white;
        }

        .login-signup {
            position: absolute;
            top: 20px;
            right: 20px;
            z-index: 10; /* Ensure it is above the image */
        }

        .login-signup a {
            color: white;
            text-decoration: none;
            margin-left: 15px;
            font-size: 16px;
        }

        .login-signup a:hover {
            text-decoration: underline;
        }

        .link-main-home {
            color: #000;
        }

        /* Media queries for responsiveness */
        @media (max-width: 768px) {
            .nav-overlay {
                flex-direction: column;
                align-items: center;
            }

            .nav-overlay ul {
                flex-direction: column;
                align-items: center;
            }

            .nav-overlay li {
                margin: 10px 0;
            }

            .shop-button {
                font-size: 18px;
                padding: 15px 30px; /* Ensure the padding remains consistent on smaller screens */
            }

            .logo img {
                height: 60px; /* Adjust logo size for smaller screens */
            }

            .hero-section p {
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <section class="hero-section">
        <img src="/images/Black-bottle.jpg" alt="Product Image" class="hero-image">
        <div class="nav-overlay">
            <div class="logo">
                <a href="/">
                    <img src="/Logo/5313c5dfeae341de827bdfebb4154726.png" alt="Logo" style="height: 80px; border-radius:50px;">
                </a>
            </div>
            <div class="login-signup">
                <a style="font-weight: 900;" href="/login">Login/Signup</a>
               
            </div>
        </div>
        <a style="text-decoration: none;" class="shop-button" href="/userProductlist">SHOP now<i style="color: white;" class="lni lni-pointer-right"></i></a>
        <p>Design & Developed by : <span style="color: #b31111;"> SALIH V</span></p>
    </section>
</body>
</html>









