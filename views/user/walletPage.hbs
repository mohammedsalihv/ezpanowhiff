<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wallet Page</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css?family=Open+Sans:400,300,300italic,400italic,600,600italic,700,700italic,800');

        .column1 {
            border-top-left-radius: 5px;
            border-bottom-left-radius: 5px;
            padding: 60px;
            background-color: #f2f2f2;
        }

        .column1 h1 span img {
            width: 30px;
            height: 30px;
            cursor: pointer;
        }

        .credit:hover {
            box-shadow: 1px 1px 0px rgba(0, 0, 0, 0.1);
            border: 4px solid #8393ca;
        }

        .credit {
            border-radius: 5px;
            border: 2px solid #e1e1e1;
            margin: 30px auto;
            background-color: #fff;
            text-align: center;
            padding: 20px;
        }

        .credit img {
            max-width: 30%;
        }

        .column2 {
            padding: 60px 30px 70px 60px;
            box-shadow: -6px 6px 20px rgba(0, 0, 0, 0.3);
            border-radius: 5px;
            background-color: #fff;
        }

        .column2-title2 span {
            font-size: 30px;
        }

        .transaction-info h3 {
            font-size: 18px;
            color: #333333;
            font-weight: 600;
        }

        .transaction-info p {
            font-size: 14px;
            color: #999999;
            margin: -14px 0 5px 0;
        }

        .transaction span {
            font-size: 18px;
            color: #8393ca;
        }

        .transaction sup {
            font-size: 10px;
        }

        .transaction {
            font-family: 'Open Sans', sans-serif;
            padding: 20px;
        }

        .transaction img {
            width: 30px;
            height: 30px;
        }

        /* Existing styles */
        .pagination-container {
            margin-top: 20px;
        }

        .pagination {
            display: inline-block;
        }

        .pagination li {
            display: inline;
            padding: 0 5px;
        }

        .pagination li a {
            color: black;
            text-decoration: none;
        }

        .pagination li.active a {
            font-weight: bold;
        }
    </style>
</head>
<header class="header-area header-style-1">
    <div id="main-header-higlights" style="background-color:#ffffff; border:1px solid #ffffff; color:black"
        class="header-top header-top-ptb-1 d-none d-lg-block">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-xl-3 col-lg-4">
                    <div style="color: #ffffff;" class="header-info">
                        <ul>
                            <li id="header-li-bg-clr">
                                <a style="color: black; font-weight:900;" href="/userHome">Home</a>
                            </li>
                            <li id="header-li-bg-clr"><a style="color: black; font-weight:900;"
                                    href="/userProductlist">Shop </a>
                            </li>
                            <li id="header-li-bg-clr">
                                <a style="color: black; font-weight:900;" href="/userAccount">Account</a>
                            </li>
                        </ul>
                    </div>
                </div>
                <div class="col-xl-6 col-lg-4">
                    <div class="text-center">
                        <div style="color: black;" id="news-flash" class="d-inline-block">
                            <ul>

                            </ul>
                        </div>
                    </div>
                </div>
                <div class="col-xl-3 col-lg-4">
                    <div class="header-info header-info-right">
                        <ul>

                            <div class="header-action-right">
                                <div class="header-action-2">
                                    <div class="header-action-icon-2">
                                        <a href="/wishlistPage">
                                            <img class="svgInject" alt="Evara"
                                                src="assets/imgs/theme/icons/icon-heart.svg">
                                            <span style="background-color: #ffffff; color:rgb(255, 255, 255)"
                                                class="pro-count "></span>
                                        </a>
                                    </div>
                                    <div class="header-action-icon-2">
                                        <a class="mini-cart-icon" href="/cart">
                                            <img alt="Evara" src="assets/imgs/theme/icons/icon-cart.svg">
                                            <span style="background-color: #ffffff; color:rgb(255, 255, 255)"
                                                class="pro-count #"></span>
                                        </a>

                                    </div>
                                </div>
                            </div>
                            {{#if this.userLogged}}
                            <div class="d-flex justify-content-right ml-4 mt-2">
                                <button style="background: transparent; border:none" onclick="logoutConfirmation()"
                                    type="button">
                                    <i style="color: #000000; font-size:x-large; " class="fa-solid fa-user"></i><i
                                        style="color: #000000; font-size:small"
                                        class="fa-solid fa-right-from-bracket"></i>
                                </button>
                            </div>
                            {{else}}
                            <li>
                                <div style="margin-left: 42%;" class="d-flex justify-content-right "></div>
                                <i class="fi-rs-user"></i><a style="color: #046963;" href="/login">Log In / Sign Up</a>
                    </div>
                    </li>
                    {{/if}}
                    </ul>
                </div>
            </div>
        </div>
    </div>
    </div>
</header>

<body>
    <div class="container">
        <div class="row  mt-4">
            <div class="col-md-6 column1">
                <h5> <span id="span-wallet">My Wallets</span> <span>
                        <input id="addMoneyInput" name="addTowallet" class="text-center mt-4" type="number">
                        <button style="color: #e1e1e1;" onclick="addMoney()" class="btn-addMoney">Add money</button>
                </h5>
                <div class="credit visa">

                    <h2>**** **** ****</h2>
                    <p>No such documents</p>
                </div>

            </div>

            <div class="col-md-6 column2">
                <div class="d-flex justify-content-between">
                    <h5>Current Balance : <span>{{walletData.balance}}.00<sup></sup></span></h5>
                    <h1 class="column2-title2"></h1>
                </div>
                <hr>

                <div class="transaction">
                    {{#if isZero}}
                    <h6 style="margin-bottom: 4%;" class="text-center"> No Transactions</h6>
                    {{else}}
                    <h6 style="margin-bottom: 4%;" class="text-center"> Transactions</h6>
                    {{/if}}
                    {{#each transactions}}
                    <div style="margin-bottom: 4%;" class="transaction-info">
                        <div class="row mb-3">
                            <div class="col d-flex align-items-center">
                                <i style="margin-right: 1%; color: rgb(3, 3, 3);" class="lni lni-checkmark-circle"></i>
                                <span style="font-size: large;">{{this.description}}</span>
                            </div>

                            <div class="col text-right d-flex align-items-center justify-content-end">
                                {{#if this.canceled}}
                                <br>
                                <span class="bg badge-primary" id="bg-wallet"
                                    style="color: #ffffff; font-weight: 500; margin-right: 5%;">
                                    {{this.canceled}}
                                </span><sup></sup>
                                {{else}}
                                {{#if this.returned}}
                                <br>
                                <span class="bg badge-primary" id="bg-wallet"
                                    style="color: #ffffff; font-weight: 500; margin-right: 5%;">
                                    {{this.returned}}
                                </span><sup></sup>
                                {{/if}}
                                {{/if}}
                                <span id="bg-wallet" style="color: #000000; font-weight: 700;">
                                    {{this.amount}}.00
                                </span>
                            </div>


                        </div>
                        {{#if (eq this.type 'credited')}}
                        <p style="font-weight: 900;" class="col-lg-12 ml-1">
                            <span id="btn-type-credit">{{this.type}}</span>
                        </p>
                        {{else}}
                        <p style="font-weight: 900;" class="col-lg-12 ml-1">
                            <span id="btn-type-debit">{{this.type}}</span>
                        </p>
                        {{/if}}
                    </div>
                    {{/each}}
                </div>

            </div>
        </div>
    </div>
    <section class="d-flex justify-content-center" style="height: 10vh;  color:#fff">
        <div class="pagination-wrap">
            <ul class="pagination">
                {{#range 1 totalPages}}
                <li style="background-color: #ffffff;" class="page-item {{#if (eq ../page this)}}active{{/if}}">
                    <a style="background-color: #ffffff; color:#000000" class="page-link"
                        href="walletPage?page={{this}}&limit={{../limitRow}}"><strong>{{this}}</strong></a>
                </li>
                {{/range}}
            </ul>
        </div>
    </section>


    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>



    <script>

        async function addMoney() {
            const inputAmount = document.getElementById('addMoneyInput');
            const amount = parseFloat(inputAmount.value);

            if (isNaN(amount) || amount <= 0) {
                alert('Please enter a valid amount.');
                return;
            }

            try {
                const response = await fetch('/addToWallet', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ amount }),
                });

                const data = await response.json();
                console.log('Order creation response:', data);

                if (!data || !data.key_id || !data.order) {
                    throw new Error('Invalid order creation response');
                }

                const options = {
                    key: data.key_id,
                    amount: data.order.amount, // Amount in paise
                    currency: data.order.currency,
                    order_id: data.order.id,
                    name: "Ezpano Whiff",
                    description: "Adding amount to wallet",
                    handler: async function (response) {
                        console.log('Razorpay success response:', response);

                        const verificationData = {
                            amount: data.order.amount,
                            orderCreationId: data.order.id,
                            razorpayPaymentId: response.razorpay_payment_id,
                            razorpayOrderId: response.razorpay_order_id,
                            razorpaySignature: response.razorpay_signature,
                        };

                        try {
                            const verifyResponse = await fetch(`/razorpay-verify-wallet`, {
                                method: 'POST',
                                headers: {
                                    'Content-Type': 'application/json',
                                },
                                body: JSON.stringify({ data: verificationData }),
                            });

                            const result = await verifyResponse.json();
                            console.log("Verification result:", result);

                            if (result.message === 'Payment added to wallet successfully') {
                                Swal.fire({
                                    icon: 'success',
                                    title: 'Payment Successful',
                                    text: 'Amount added to wallet successfully!',
                                }).then(() => {
                                    window.location.reload();
                                });
                            } else {
                                Swal.fire({
                                    icon: 'error',
                                    title: 'Verification Error',
                                    text: 'Some verification issue occurred. Please try again after some time.',
                                });
                            }
                        } catch (error) {
                            console.error("Error during verification:", error);
                            Swal.fire({
                                icon: 'error',
                                title: 'Verification Error',
                                text: `An error occurred during verification: ${error.message}`,
                            });
                        }
                    },
                    prefill: {
                        name: "Soumya Dey",
                        email: "SoumyaDey@example.com",
                        contact: "9999999999",
                    },
                    notes: {
                        address: "Soumya Dey Corporate Office",
                    },
                    theme: {
                        color: "#000",
                    },
                };

                const rzp = new Razorpay(options);
                rzp.open();
            } catch (error) {
                console.error('Error adding amount to wallet:', error);
                Swal.fire({
                    icon: 'error',
                    title: 'Payment Error',
                    text: 'An error occurred while initiating the payment. Please try again.',
                });
            }
        }

    </script>

    <script>

        function logoutConfirmation() {
            Swal.fire({
                title: 'Are you sure?',
                text: "You will be logged out!",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                confirmButtonText: 'Yes, log out!'
            }).then((result) => {
                if (result.isConfirmed) {
                    Swal.fire(
                        'Logged out!',
                        'You have been logged out.',
                        'success'
                    ).then(() => {
                        window.location.href = "/userLogout";
                    });
                } else {
                    window.location.href = "/userHome";
                }
            });
        }
    </script>

</body>

</html>






